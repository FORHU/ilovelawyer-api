/** Re-export of the Prisma-generated enum — the single source of truth for the app-level
 * jurisdiction type. Belongs to Organization only; never to User. See Organization.jurisdiction
 * in prisma/schema.prisma. */
export type { Jurisdiction } from "@prisma/client";
